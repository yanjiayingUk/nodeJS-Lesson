// va a=1;//SyntaxError(可能是某个关键字错误)

// console.log(a);//ReferenceError(使用的变量没有实际声明，在某一位置使用了未定义的变量)
const http=require("http");
html.createServer(function(req,res){//TypeError
    req.write();
}).listen(8081)